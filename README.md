1️⃣What is the difference between var, let, and const?


Answer:: var, let, and const in JavaScript differ mainly in scope and reassignability. var is function-scoped. let and const are block-scoped

2️⃣ What is the spread operator (...)?

Answer:: The spread operator (...) is ES6 syntax that expands iterables  arrays or strings. into individual elements or unpacks object properties

3️⃣ What is the difference between map(), filter(), and forEach()?

Answer:: map() will return a new array as per the conditions applied. forEach() will not return anything. forEach() returns undefined.
filter()method will return an array of matching elements else will return an empty array if no matching happens.



4️⃣ What is an arrow function?


Answer:: Arrow functions are a concise ES6 syntax for writing functions, utilizing the (=> arrow) operator


5️⃣ What are template literals?


Answer:: Template literals are a (ES6) feature that allow for easier string manipulation.Template literals is start (``) and insied the Template wirte aney html eliment and also calld js variable
